<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Sovereign Botton</title>
    <style>
        /* Stack de fontes para SF Pro (San Francisco) */
        body {
            background-color: #000;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            font-family: -apple-system, BlinkMacSystemFont, "SF Pro Display", "SF Pro Text", "Helvetica Neue", Helvetica, Arial, sans-serif;
        }

        /* Animação da Seta: vai para a direita e volta */
        @keyframes arrow-nudge {
            0%, 100% { transform: translateX(0); }
            50% { transform: translateX(8px); }
        }

        .group:hover .animate-indicate {
            animation: arrow-nudge 0.8s ease-in-out infinite;
        }

        /* Giro do Feixe */
        @keyframes beam-spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        /* Movimento dos Pontos */
        @keyframes dots-move { 
            0% { background-position: 0 0; } 
            100% { background-position: 24px 24px; } 
        }
    </style>
</head>
<body class="bg-black">

    <button class="group flex overflow-hidden uppercase transition-all duration-500 hover:scale-[1.03] hover:shadow-[0_0_60px_-15px_rgba(59,130,246,0.6)] focus:outline-none text-sm font-semibold text-white tracking-[0.2em] rounded-full pt-5 pr-12 pb-5 pl-12 relative items-center justify-center border border-white/5">
        
        <!-- Borda Animada (Feixe Azul Ultra Forte) -->
        <div class="absolute inset-0 -z-20 rounded-full overflow-hidden p-[1.5px]">
            <!-- Gradiente Azul Vibrante (#3b82f6 para #60a5fa) -->
            <div class="absolute inset-[-250%] bg-[conic-gradient(from_0deg,transparent_0_200deg,#3b82f6_280deg,#60a5fa_320deg,transparent_360deg)]" style="animation: beam-spin 2s linear infinite;"></div>
            <!-- Fundo para criar o recorte da borda -->
            <div class="absolute inset-[1px] rounded-full bg-black"></div>
        </div>

        <!-- Corpo do Botão e Efeitos de Luz -->
        <div class="-z-10 overflow-hidden bg-black rounded-full absolute inset-[1.5px]">
            <!-- Esfumaçado/Radial Branco Central -->
            <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(255,255,255,0.12)_0%,transparent_70%)]"></div>
            
            <!-- Padrão de Pontos (Sutil no fundo escuro) -->
            <div class="opacity-20 absolute inset-0" style="background-image: radial-gradient(rgba(255,255,255,0.3) 1px, transparent 1px); background-size: 14px 14px; animation: dots-move 12s linear infinite"></div>
            
            <!-- Glow Branco Lateral no Hover -->
            <div class="absolute inset-0 bg-white/5 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
        </div>

        <!-- Conteúdo -->
        <span class="relative z-10 text-white/90 transition-all duration-300 group-hover:text-white group-hover:drop-shadow-[0_0_8px_rgba(255,255,255,0.5)]">
            Sovereign Botton
        </span>
        
        <!-- Seta Branca com Animação Corrigida -->
        <div class="relative z-10 ml-3 transition-all duration-300 animate-indicate">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M5 12h14"></path>
                <path d="m12 5 7 7-7 7"></path>
            </svg>
        </div>
    </button>

</body>
</html>
